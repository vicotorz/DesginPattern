命令模式：将请求封装为命令基于事件驱动异步执行，以实现命令的发送者和命令的执行者之间的解耦

主要角色：
抽象命令类：执行命令的接口，定义执行命令的抽象方法 execute()
具体命令类：抽象命令类的实现类，持有接受者对象，并在接收到命令后调用命令执行者的方法action()实现命令的调用和执行
命令执行者：命令的具体执行者，定义了命令执行的具体方法action()
命令调用者：接收客户端的命令并异步执行